[package]
name = "kinetix-core"
version = "0.1.0"
edition = "2024"

[dependencies]
kinetix-spatial = { path = "../kinetix-spatial" }
nalgebra = { version = "0.34", default-features = false, features = ["macros"] }
